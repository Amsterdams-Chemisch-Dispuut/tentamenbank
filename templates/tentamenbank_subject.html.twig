<article class="tentamenbank-container">
<div class="alert alert-warning" role="alert">{{ '<strong>DISCLAIMER:</strong> Tentamens uit het verleden kunnen geen garantie bieden voor het heden. Let vooral op bij wisseling van een vakcoördinator. Ben jij klaar met een tentamen? Of heb jij er thuis nog een aantal liggen? Lever ze in bij het ACD en help zo de tentamenbank up-to-date te houden.'|t }}</div>
<a href=".." class="btn btn-primary">&larr;</a>
<table id="subjects" class="table" style="width:100%">
    <thead class="thead-dark">
        <tr>
            <th>Type</th>
            <th>Date</th>
            <th>Questions</th>
            <th>Answers</th>
        </tr>
    </thead>
    <tbody>
        {% for exam in exams %}
        <tr>
            <th scope="row">{{ exam.type }}</td>
            <td>{{ exam.date }}</td>
            <td>
          {% if exam.questions %}
            <a href="https://acdweb-storage.s3.eu-central-1.amazonaws.com/{{ exam.questions }}" target="_blank" class="btn btn-primary">Questions</a>
          {% else %}
            <a class="btn btn-outline-primary">Questions</a>
          {% endif %}
        </td>
        <td>
          {% if exam.answers %}
            <a href="https://acdweb-storage.s3.eu-central-1.amazonaws.com/{{ exam.answers }}" target="_blank" class="btn btn-primary">Answers</a>
          {% else %}
            <a class="btn btn-outline-primary">Answers</a>
          {% endif %}
        </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
</article>